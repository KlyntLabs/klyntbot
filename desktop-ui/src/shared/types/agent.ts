// ── Coaching ──────────────────────────────────────────────

export interface CoachingIntervention {
  id: string;
  interventionType: string;
  message: string;
  triggerName: string;
  timestamp: string;
}
