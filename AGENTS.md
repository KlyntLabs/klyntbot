# AGENTS.md — Phase 4 smoke test

You are operating inside the klyntbot repo. Test sandbox: `/tmp/klynt-phase1-sandbox/`.
When asked to run shell commands, use the `bash` tool.
When asked to modify files, use the `edit` or `write` tool. Be concise.
