//! Re-export from tools-core — the canonical home for ParamExtractor.
//!
//! All tool implementations should use `ParamExtractor` via this re-export
//! or directly from `tools_core::params`.

pub use tools_core::params::ParamExtractor;
